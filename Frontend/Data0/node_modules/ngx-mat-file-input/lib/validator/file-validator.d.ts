import { ValidatorFn } from '@angular/forms';
export declare namespace FileValidator {
    /**
     * Function to control content of files
     *
     * @param bytes max number of bytes allowed
     *
     * @returns
     */
    function maxContentSize(bytes: number): ValidatorFn;
    /**
     *
     * @description Handles allowed file types by controlling whether some specific extensions matches with the uploaded file type
     * @export
     * @param {string[]} extensions
     * @returns {ValidatorFn}
     */
    function allowedExtensions(extensions: string[]): ValidatorFn;
}
