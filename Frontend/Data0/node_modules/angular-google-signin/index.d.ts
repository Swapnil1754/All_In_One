/// <reference types="gapi.auth2" />
import { AfterViewInit, EventEmitter } from '@angular/core';
export declare class GoogleSignInSuccess {
    googleUser: gapi.auth2.GoogleUser;
    constructor(googleUser: gapi.auth2.GoogleUser);
}
export declare class GoogleSignInFailure {
}
export declare class GoogleSignInComponent implements AfterViewInit {
    private id;
    private scope;
    private _width;
    width: string;
    private _height;
    height: string;
    private _longTitle;
    longTitle: string;
    private theme;
    private clientId;
    private cookiePolicy;
    private _fetchBasicProfile;
    fetchBasicProfile: string;
    private hostedDomain;
    private openidRealm;
    googleSignInSuccess: EventEmitter<GoogleSignInSuccess>;
    googleSignInFailure: EventEmitter<GoogleSignInFailure>;
    ngAfterViewInit(): void;
    private auth2Init();
    private handleFailure();
    private handleSuccess(googleUser);
    private renderButton();
}
